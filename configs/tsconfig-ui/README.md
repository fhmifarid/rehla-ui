# @rehla-ui/tsconfig

Shared TypeScript configurations for the Rehla UI monorepo.

```json
{
  "extends": "@rehla-ui/tsconfig/react-library.json"
}
```

Available presets: `base.json`, `react-library.json`, `nextjs.json`.